[build]
  base = "apps/web"
  command = "cd ../.. && npm i -g pnpm && pnpm install && pnpm build --filter @haxvibe/web"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

[[plugins]]
  package = "@netlify/plugin-nextjs"
